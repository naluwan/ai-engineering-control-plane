export function SiteFooter() {
  return (
    <footer className="border-t border-foreground/10">
      <div className="mx-auto w-full max-w-4xl px-6 py-6 text-xs leading-relaxed text-foreground/50">
        <p>
          Portfolio project. Repository foundation and product planning only —
          no agent execution, GitHub integration or LLM provider is connected.
        </p>
      </div>
    </footer>
  );
}
